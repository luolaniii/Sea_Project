spring:
  datasource:
    url: jdbc:mysql://yangfan918.cn:33123/sea_db?serverTimezone=Asia/Shanghai&characterEncoding=utf8&autoReconnect=true&zeroDateTimeBehavior=convertToNull&useSSL=true&requireSSL=false
    username: root
    password: iK1bOLOyW#@3NMJe
    type: com.alibaba.druid.pool.DruidDataSource

  redis:
    host: yangfan918.cn
    port: 6379
    password: Express112233..
    timeout: 60s
    database: 10
    lettuce:
      pool:
        # 连接池最大连接数（使用负值表示没有限制） 默认 8
        max-active: 40
        # 连接池中的最大空闲连接 默认 0
        max-idle: 30
        # 连接池中的最小空闲连接 默认 0
        min-idle: 10
        # 连接池最大阻塞等待时间（使用负值表示没有限制） 默认 -1
        max-wait: 5s
        #空闲连接回收间隔，为负时不创建空闲回收线程，为正时表示定时空闲连接回收间隔
        time-between-eviction-runs: 1s

swagger:
  enabled: true